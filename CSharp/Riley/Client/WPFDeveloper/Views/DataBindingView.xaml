<UserControl x:Class="WPFDeveloper.Views.DataBindingView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d">
    
    <UserControl.Resources>
        <SolidColorBrush x:Key="CardBackground" Color="#ffffff"/>
        <SolidColorBrush x:Key="PrimaryColor" Color="#667eea"/>
        <SolidColorBrush x:Key="TextPrimary" Color="#1a202c"/>
        <SolidColorBrush x:Key="TextSecondary" Color="#4a5568"/>
        
        <LinearGradientBrush x:Key="PrimaryGradient" StartPoint="0,0" EndPoint="1,1">
            <GradientStop Color="#667eea" Offset="0"/>
            <GradientStop Color="#764ba2" Offset="1"/>
        </LinearGradientBrush>
        
        <DropShadowEffect x:Key="CardShadow" Color="#000000" Direction="270" ShadowDepth="2" Opacity="0.1" BlurRadius="8"/>
        <FontFamily x:Key="ModernFont">Segoe UI</FontFamily>
    </UserControl.Resources>
    
    <ScrollViewer VerticalScrollBarVisibility="Auto" Background="Transparent">
        <StackPanel Margin="0,0,0,20">
            <!-- 标题卡片 -->
            <Border Background="{StaticResource PrimaryGradient}" 
                    CornerRadius="16" 
                    Padding="32,24" 
                    Margin="0,0,0,24"
                    Effect="{StaticResource CardShadow}">
                <StackPanel>
                    <TextBlock Text="🔗 WPF 数据绑定技术" 
                               FontSize="28" 
                               FontWeight="Bold" 
                               Foreground="White"
                               FontFamily="{StaticResource ModernFont}"
                               HorizontalAlignment="Center"
                               Margin="0,0,0,8"/>
                    <TextBlock Text="学习 WPF 数据绑定的各种用法" 
                               FontSize="16" 
                               Foreground="White" 
                               Opacity="0.9"
                               FontFamily="{StaticResource ModernFont}"
                               HorizontalAlignment="Center"/>
                </StackPanel>
            </Border>
            
            <!-- 内容卡片 -->
            <Border Background="{StaticResource CardBackground}" 
                    CornerRadius="12" 
                    Padding="24" 
                    Effect="{StaticResource CardShadow}">
                <StackPanel>
                    <TextBlock Text="自动注册成功！" 
                               FontSize="20" 
                               FontWeight="SemiBold"
                               FontFamily="{StaticResource ModernFont}"
                               Foreground="{StaticResource TextPrimary}"
                               HorizontalAlignment="Center"
                               Margin="0,0,0,16"/>
                    
                    <TextBlock Text="这个视图通过 [View] 特性自动注册到 DI 容器中，无需手动配置。" 
                               FontSize="14"
                               FontFamily="{StaticResource ModernFont}"
                               Foreground="{StaticResource TextSecondary}"
                               HorizontalAlignment="Center"
                               TextWrapping="Wrap"
                               TextAlignment="Center"/>
                </StackPanel>
            </Border>
        </StackPanel>
    </ScrollViewer>
</UserControl>
