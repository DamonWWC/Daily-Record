###
# Riley.Server 用户管理API测试
# 测试新的用户管理功能（已移动到Auth模块）

### Variables
@baseUrl = http://localhost:5000
@email = test@example.com
@password = password123

### 1. 注册测试用户
POST {{baseUrl}}/api/auth/register
Content-Type: application/json

{
  "username": "testuser",
  "email": "{{email}}",
  "password": "{{password}}",
  "name": "Test User"
}

### 2. 用户登录
POST {{baseUrl}}/api/auth/login
Content-Type: application/json

{
  "username": "testuser",
  "password": "{{password}}"
}

### 3. 获取所有用户（需要认证）
GET {{baseUrl}}/api/users
Authorization: Bearer {{token}}

### 4. 根据ID获取用户（需要认证）
GET {{baseUrl}}/api/users/10
Authorization: Bearer {{token}}

### 5. 创建新用户（需要认证）
POST {{baseUrl}}/api/users
Authorization: Bearer {{token}}
Content-Type: application/json

{
  "username": "newuser",
  "name": "New User",
  "email": "newuser@example.com",
  "password": "newpassword123",
  "phone": "1234567890",
  "role": "User",
  "isActive": true
}

### 6. 更新用户信息（需要认证）
PUT {{baseUrl}}/api/users/2
Authorization: Bearer {{token}}
Content-Type: application/json

{
  "name": "Updated User Name",
  "email": "updated@example.com",
  "phone": "9876543210",
  "role": "Admin",
  "isActive": true
}

### 7. 检查邮箱是否存在
GET {{baseUrl}}/api/users/check-email?email=test@example.com
Authorization: Bearer {{token}}

### 8. 检查用户名是否存在
GET {{baseUrl}}/api/users/check-username?username=testuser
Authorization: Bearer {{token}}

### 9. 删除用户（软删除，需要认证）
DELETE {{baseUrl}}/api/users/2
Authorization: Bearer {{token}}

### 10. 获取用户信息（验证JWT）
GET {{baseUrl}}/api/auth/me
Authorization: Bearer {{token}}

###
# 说明：
# 1. 首先需要注册或登录获取token
# 2. 将获取到的token替换{{token}}变量
# 3. 所有用户管理操作都需要有效的JWT token
# 4. 用户删除是软删除，只会将IsActive设置为false
# 5. Auth模块现在包含了完整的用户认证和管理功能
