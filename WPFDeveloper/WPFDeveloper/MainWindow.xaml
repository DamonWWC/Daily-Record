﻿﻿﻿﻿﻿﻿<Window x:Class="WPFDeveloper.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:hc="https://handyorg.github.io/handycontrol"
        xmlns:local="clr-namespace:WPFDeveloper"
        mc:Ignorable="d"
        Title="{Binding PageTitle}" Height="720" Width="1280"
        WindowStartupLocation="CenterScreen">
    
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="220"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>

        <!-- 左侧导航菜单 -->
        <Border Background="#1C1C1C">
            <StackPanel>
                <TextBlock Text="WPF开发者工具" 
                           Foreground="White"
                           FontSize="20"
                           FontWeight="Bold"
                           Margin="20,30,20,30"
                           HorizontalAlignment="Center"/>

                <ListBox Background="Transparent" 
                         BorderThickness="0"
                         Margin="0,20,0,0">
                    <ListBoxItem Style="{StaticResource ListBoxItemCustom}" 
                                Command="{Binding NavigateCommand}"
                                CommandParameter="Home">
                        <StackPanel Orientation="Horizontal">
                            <hc:Icon Type="Home" Margin="0,0,10,0"/>
                            <TextBlock Text="首页"/>
                        </StackPanel>
                    </ListBoxItem>

                    <ListBoxItem Style="{StaticResource ListBoxItemCustom}"
                                Command="{Binding NavigateCommand}"
                                CommandParameter="Settings">
                        <StackPanel Orientation="Horizontal">
                            <hc:Icon Type="Settings" Margin="0,0,10,0"/>
                            <TextBlock Text="设置"/>
                        </StackPanel>
                    </ListBoxItem>

                    <ListBoxItem Style="{StaticResource ListBoxItemCustom}"
                                Command="{Binding NavigateCommand}"
                                CommandParameter="About">
                        <StackPanel Orientation="Horizontal">
                            <hc:Icon Type="Info" Margin="0,0,10,0"/>
                            <TextBlock Text="关于"/>
                        </StackPanel>
                    </ListBoxItem>
                </ListBox>
            </StackPanel>
        </Border>

        <!-- 右侧内容区域 -->
        <Frame Grid.Column="1" 
               x:Name="MainFrame"
               NavigationUIVisibility="Hidden"/>
    </Grid>
</Window>