
# XAML 编码约定

## 命名空间声明
始终按正确顺序包含这些标准 XAML 命名空间：
```xml
xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
xmlns:local="clr-namespace:WPFDeveloper"
```

## 格式化规则
- 使用 4 个空格进行缩进（不使用制表符）
- 对于复杂元素，将每个属性放在单独的行上
- 对所有元素名称和属性使用 PascalCase
- 为 x:Name 属性使用有意义的名称

## 布局指南
- 对于大多数布局，优先使用 Grid 而不是 Canvas
- 根据内容使用适当的面板类型（StackPanel、DockPanel、WrapPanel）
- 仅在必要时设置明确的 Width/Height
- 适当使用 Margin 和 Padding

## 资源组织
- 在逻辑资源字典中定义样式和模板
- 为资源使用有意义的键
- 将相关资源分组在一起
- 考虑为大型应用程序使用合并字典

## 数据绑定最佳实践
- 使用正确的绑定语法：`{Binding PropertyName}`
- 在需要时指定绑定模式：`{Binding PropertyName, Mode=TwoWay}`
- 适当使用相对源
- 考虑为复杂数据转换使用转换器
- Specify binding modes when needed: `{Binding PropertyName, Mode=TwoWay}`
- Use relative sources appropriately
- Consider using converters for complex data transformations