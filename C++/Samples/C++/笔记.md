1. 最好提供一个显示的默认构造函数。
2. 复制构造函数。 只要代码中实现动态内存分配（new）
3. 赋值运算符。只要编写了复制构造函数，最好就编写赋值构造函数。
4. 基类就要写虚析构函数。
5. 编写使用对象作为参数的函数时，最好使用引用。
6. 友元函数不是类的成员函数，所以不能使用this指针。不能继承，如果派生类要使用基类的友元，可以通过强制类型转换为基类，就可用用了。
7. 单字符输入功能，cin.get(char&) 提供不跳过控制的单字符
8. 特殊成员函数包括默认构造函数、复制构造函数、复制赋值运算符、析构函数、移动构造函数、移动赋值运算符。